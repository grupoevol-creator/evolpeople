{
  "name": "evolpeople-push-server",
  "version": "1.0.0",
  "description": "Serviço HTTP (Google Cloud Function) que envia notificações Web Push para o EvolPeople, chamado pelo Code.gs.",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
